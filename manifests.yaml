---
# Source: apps-template/templates/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: 'team-hatchback-config'
  labels:
    app.kubernetes.io/name: 'team-hatchback'
data:
  application.name: 'team-hatchback'  
  more.info: 'This is a configmap for team-hatchback application'
---
# Source: apps-template/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: svc-stable
  annotations:
    prometheus.io/fastscrape: 'true'
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: 'team-hatchback-app'
---
# Source: apps-template/templates/rollout.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "team-hatchback-app"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "team-hatchback-app"
  template:
    metadata:
      labels:
        app: "team-hatchback-app"
    spec:
      containers:
        - image: "ghcr.io/akuity/sedemo-monorepo-rollouts-app:50-blue"
          name: "team-hatchback-app"
          ports:
            - containerPort: 8080
          env:
            - name: SEMVER
              value: "50-blue"
            - name: STAGE
              value: dev
            - name: NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            
  minReadySeconds: 30
  revisionHistoryLimit: 3
---
# Source: apps-template/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: stable-ingress
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - "dev.hatchback.akpdemoapps.link"
    secretName: tls-secret
  rules:
  - host: "dev.hatchback.akpdemoapps.link"
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: svc-stable
            port:
              number: 80
